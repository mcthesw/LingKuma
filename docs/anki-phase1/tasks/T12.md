# T12 — 实现一次性设置与就绪检查

状态：TODO。
依赖：T04, T05, T06, T11。

## 开始前

阅读 `START_HERE.md`、本任务相关的 `SPEC.md` / `ARCHITECTURE.md` / `API_CONTRACTS.md` 条款，以及 `ACCEPTANCE.md` 中下列用例。先检查依赖任务证据，不凭口头报告跳过。

## 修改范围

manager/settings UI、受信消息路由、配置测试。

## 实施步骤

1. 提供连接测试、牌组选择、专用类型ensure/verify、固定语言、自动摘录暂停/启用、可选音频。
2. 完成一次性设置即启用正常自动流程；已有未配置摘录自动继续，无逐条保存动作。
3. 凭证只存新DB meta并仅后台使用；设置页public DTO不反回完整key，不进入原WebDAV/导出。
4. 更改目标只用于新记录；旧队列有明确绑定，不默默跨profile/换牌组写入。
5. 只通过可信扩展页消息执行model创建/设置修改，内容脚本无权触发。

## 必须验证

- A31：设置一次后查询不弹确认、旧未配置记录能继续。
- A10/A12：错误profile/模型/牌组说明和修复。

## 明确禁止

不为每条笔记打开Anki添加窗口，不重复要求AI配置，不提供多端同步开关。

## 完成条件

上述行为通过相应测试，既有回归未倒退，修改限于本任务范围。将真实命令/结果/未运行项写入 `PROGRESS.md`，独立提交。若实际API或构建能力与设计不符，记录证据和最小调整；不得静默改产品行为或删验收项。

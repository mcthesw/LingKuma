# T07 — 提取无 DOM 依赖的语境解释适配器

状态：TODO。
依赖：T00, T02。

## 开始前

阅读 `START_HERE.md`、本任务相关的 `SPEC.md` / `ARCHITECTURE.md` / `API_CONTRACTS.md` 条款，以及 `ACCEPTANCE.md` 中下列用例。先检查依赖任务证据，不凭口头报告跳过。

## 修改范围

provider-adapter.js；现有AI传输/配置读取的最小提取；provider测试。

## 实施步骤

1. 沿用T00确认的provider配置/鉴权/请求方法，不要求用户再配置另一份相同API。
2. 提供不依赖window/highlightManager/tooltipEl的explainInContext接口；不能加载整份a3到worker。
3. 使用专用结构化提示和返回校验；meaning必填，翻译/reading/usage可空。优先已有协议能力，兼容不支持特定JSON schema参数的提供方。
4. 只发送选中目标及有界上下文；原文作为不可信材料而非指令；不发送URL、Cookie、词库或凭证内容给模型。
5. 原功能的自定义提示词和provider配置保持原义；为新功能提取的共享请求要有原路径回归测试。

## 必须验证

- A16：有效释义/缺可选字段/无效JSON/错误占位符/限流/中断。
- A17：原文注入指令不能改变任务协议或输出身份/URL/动作。

## 明确禁止

不爬popup HTML作为释义，不新增付费服务依赖，不改变原文/Term来迎合AI。

## 完成条件

上述行为通过相应测试，既有回归未倒退，修改限于本任务范围。将真实命令/结果/未运行项写入 `PROGRESS.md`，独立提交。若实际API或构建能力与设计不符，记录证据和最小调整；不得静默改产品行为或删验收项。

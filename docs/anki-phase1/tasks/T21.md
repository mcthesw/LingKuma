# T21 — 交付文档、构建产物与回归门禁

状态：TODO。
依赖：T20。

## 开始前

阅读 `START_HERE.md`、本任务相关的 `SPEC.md` / `ARCHITECTURE.md` / `API_CONTRACTS.md` 条款，以及 `ACCEPTANCE.md` 中下列用例。先检查依赖任务证据，不凭口头报告跳过。

## 修改范围

README/操作手册/开发说明/CI与release smoke记录。

## 实施步骤

1. 说明一次性设置、自动查词保存、重复/新语境、状态解释、音频能力、异常处理、备份恢复和停止管理语义。
2. 清楚声明Anki负责跨设备同步和复习；本插件不承诺两台未同步Anki的全局唯一或CAS级并发编辑安全。
3. 记录构建/测试/最小模块边界/数据schema/接口；提供脱敏诊断信息。
4. 把已稳定的unit/integration/build检查加入现有CI，不要求CI运行用户真实Anki；真实浏览器/Anki步骤另列。
5. 检查clean build和实际加载，更新PROGRESS与验收结果。全部22项完成后才标记一期完成。

## 必须验证

- A50：新使用者依照说明一次设置后即可零额外操作摘录。
- 最终记录包含构建、自动化、真实Anki、Firefox、音频及未解决限制。

## 明确禁止

不发布虚假完整兼容声明，不把未运行的测试改成通过。

## 完成条件

上述行为通过相应测试，既有回归未倒退，修改限于本任务范围。将真实命令/结果/未运行项写入 `PROGRESS.md`，独立提交。若实际API或构建能力与设计不符，记录证据和最小调整；不得静默改产品行为或删验收项。

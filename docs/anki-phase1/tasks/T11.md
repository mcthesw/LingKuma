# T11 — 实现后台调度、退避和 worker 恢复

状态：TODO。
依赖：T08, T10。

## 开始前

阅读 `START_HERE.md`、本任务相关的 `SPEC.md` / `ARCHITECTURE.md` / `API_CONTRACTS.md` 条款，以及 `ACCEPTANCE.md` 中下列用例。先检查依赖任务证据，不凭口头报告跳过。

## 修改范围

runtime.js 的执行器/alarms、lease协调、fake-clock测试。

## 实施步骤

1. 新记录/设置修复/启动/打开管理页触发drain，支持alarms唤醒并在每次worker启动检查重要闹钟。
2. Anki写串行、AI最多2个并发；分批预算、有限租约、单次网络超时，全部I/O在事务外。
3. Anki离线采用全局连接退避，不逐条发同样失败请求；恢复后自动继续。
4. 鉴权/字段/配置错误blocked；AI重试有界，Anki暂不可用可低频持续恢复；没有永久keepalive或轮询风暴。
5. 启动恢复未确认写和过期job；只有当前lease owner能提交结果。

## 必须验证

- A28：重启/休眠/闹钟丢失/lease到期恢复。
- A29：大队列离线不产生每词重试风暴，恢复自动推进。
- A30：配置修复后恢复，永久错误不无限消耗AI。

## 明确禁止

不能仅依赖内存队列、setInterval或长期打开popup维持任务。

## 完成条件

上述行为通过相应测试，既有回归未倒退，修改限于本任务范围。将真实命令/结果/未运行项写入 `PROGRESS.md`，独立提交。若实际API或构建能力与设计不符，记录证据和最小调整；不得静默改产品行为或删验收项。

# T20 — 完成故障注入与真实链路验收

状态：TODO。
依赖：T19。

## 开始前

阅读 `START_HERE.md`、本任务相关的 `SPEC.md` / `ARCHITECTURE.md` / `API_CONTRACTS.md` 条款，以及 `ACCEPTANCE.md` 中下列用例。先检查依赖任务证据，不凭口头报告跳过。

## 修改范围

tests/anki集成/E2E、真实Anki测试记录；仅修复本期缺陷。

## 实施步骤

1. 实现ACCEPTANCE全部自动化用例，用fault injection覆盖每个“请求前/远端成功后/本地确认前”中断点。
2. 连续快速点击、重复点击、不同语境、关闭popup/标签页、kill worker、离线恢复、手工编辑、远端修改/删除、未知写入结果全部覆盖。
3. 用专用测试牌组/类型跑真实Anki end-to-end，确认noteId/cardId稳定，内容更新没有变更复习字段；测试清理只清理本轮创建记录。
4. 对Chrome/Edge家族与Firefox至少各一次实际加载测试，明确自动化与人工覆盖，不把一种内核的测试冒充另一种。
5. 测试“用户额外点击保存次数=0”“正常查询一次语境AI调用=1”“生产sync及排程写调用=0”。

## 必须验证

- A00～A48：有真实证据或明确标未运行，不能带未完成必测项宣布一期交付。
- A49：完整一次性设置→查词→本地→Anki→编辑→异常→恢复→备份走通。

## 明确禁止

不为通过测试降低产品行为；不使用用户主牌组做破坏性测试。

## 完成条件

上述行为通过相应测试，既有回归未倒退，修改限于本任务范围。将真实命令/结果/未运行项写入 `PROGRESS.md`，独立提交。若实际API或构建能力与设计不符，记录证据和最小调整；不得静默改产品行为或删验收项。

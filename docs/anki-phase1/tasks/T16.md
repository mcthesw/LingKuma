# T16 — 实现摘录管理、编辑与异常处理

状态：TODO。
依赖：T10, T12, T15。

## 开始前

阅读 `START_HERE.md`、本任务相关的 `SPEC.md` / `ARCHITECTURE.md` / `API_CONTRACTS.md` 条款，以及 `ACCEPTANCE.md` 中下列用例。先检查依赖任务证据，不凭口头报告跳过。

## 修改范围

manager页、业务命令UI、管理E2E。

## 实施步骤

1. 分页搜索/按词归组/按状态筛选，显示真实原句、释义、来源、最后核实时间和音频状态。
2. 编辑单条材料及个人笔记，自动排队原地更新；修改前expectedRevision校验。
3. 差异页并排展示本地与Anki，支持保留Anki/用本地/选字段；处理前重新读远端。正常查询流程不出现此页。
4. 实现remote_missing显式重建或停止管理、multiple matches错误详情、打开关联Anki笔记。
5. 停止管理保留身份标记、不删Anki；再次查询不自动复活；显式恢复才重新管理。

## 必须验证

- A39：管理搜索/编辑/异常处理不产生额外capture。
- A25/A27：冲突/缺失处理正确。
- A40：排除后重查不复活、不隐式删Anki。

## 明确禁止

不加入复习页面、进度仪表盘或普通查词必经编辑表单。

## 完成条件

上述行为通过相应测试，既有回归未倒退，修改限于本任务范围。将真实命令/结果/未运行项写入 `PROGRESS.md`，独立提交。若实际API或构建能力与设计不符，记录证据和最小调整；不得静默改产品行为或删验收项。

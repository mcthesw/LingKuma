# T02 — 冻结数据契约和确定性摘录身份

状态：TODO。
依赖：T01。

## 开始前

阅读 `START_HERE.md`、本任务相关的 `SPEC.md` / `ARCHITECTURE.md` / `API_CONTRACTS.md` 条款，以及 `ACCEPTANCE.md` 中下列用例。先检查依赖任务证据，不凭口头报告跳过。

## 修改范围

contracts.js、identity.js、纯函数测试。

## 实施步骤

1. 实现 originSnapshot、Capture DTO、错误码、字段名和消息外壳校验。统一字符串/大小/语言/UTF-16 offset 约束。
2. 严格实施 ARCHITECTURE 第5节身份算法；固定 JSON 数组顺序、UTF-8、SHA-256 和 lk1_前缀。
3. 实现真实选区检查和规范化位置校验；同词同句多次出现按位置区分；不转 lemma、不忽略大小写。
4. 读取包内 fixtures/identity-v1.json 的固定期望值测试，不在测试中用被测实现重新生成 expected。
5. 对 contextQuality=selection_only 明确使用 fallbackSourceKey；普通上下文不以URL/日期/AI释义作为身份。

## 必须验证

- A03：所有身份 golden vectors。
- A04：范围错误、超长输入、大小写、组合字符、emoji、短语、重复词的边界。

## 明确禁止

不通过模糊匹配、AI义项聚类、仅词去重或随机UUID替代产品身份。

## 完成条件

上述行为通过相应测试，既有回归未倒退，修改限于本任务范围。将真实命令/结果/未运行项写入 `PROGRESS.md`，独立提交。若实际API或构建能力与设计不符，记录证据和最小调整；不得静默改产品行为或删验收项。

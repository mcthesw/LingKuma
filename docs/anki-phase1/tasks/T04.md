# T04 — 实现唯一的 AnkiConnect 客户端

状态：TODO。
依赖：T02。

## 开始前

阅读 `START_HERE.md`、本任务相关的 `SPEC.md` / `ARCHITECTURE.md` / `API_CONTRACTS.md` 条款，以及 `ACCEPTANCE.md` 中下列用例。先检查依赖任务证据，不凭口头报告跳过。

## 修改范围

anki-client.js、协议fake与客户端测试。

## 实施步骤

1. 按 API_CONTRACTS 实现loopback端点校验、version=6、可选key、超时/AbortSignal、结构化错误。
2. 检查HTTP/JSON/error但允许合法null/false/[]结果和额外响应字段；未知能力明确报错。
3. 实现实际验证过的窄操作封装；查询ID只能来自已验证captureId/noteId，不接受页面输入的任意action/endpoint。
4. 读请求可以受控重试；addNote/updateNoteFields/storeMediaFile不在传输层盲目自动重试。
5. 实现profile能力检测与预期profile保护，不自动切换profile、不调用AnkiWeb同步。

## 必须验证

- A08：协议成功/错误/HTTP失败/JSON损坏/超时，null写返回正确处理。
- A09：loopback及凭证边界、重定向阻止。
- A10：profile不符停止写入；不支持检测时显示能力限制。

## 明确禁止

不在多个模块各写一套fetch，不扩大到远程Anki，不自动修改AnkiConnect或CORS配置。

## 完成条件

上述行为通过相应测试，既有回归未倒退，修改限于本任务范围。将真实命令/结果/未运行项写入 `PROGRESS.md`，独立提交。若实际API或构建能力与设计不符，记录证据和最小调整；不得静默改产品行为或删验收项。

# T00 — 记录本地基线与真实 API 能力

状态：TODO。
依赖：无。

## 开始前

阅读 `START_HERE.md`、本任务相关的 `SPEC.md` / `ARCHITECTURE.md` / `API_CONTRACTS.md` 条款，以及 `ACCEPTANCE.md` 中下列用例。先检查依赖任务证据，不凭口头报告跳过。

## 修改范围

仓库构建/manifest/查词入口/AI 请求/TTS；仅新增审计文档和只读探测脚本。

## 实施步骤

1. 检查 git status、当前分支与提交；不覆盖未提交改动、不重置到文档 SHA。记录 Node、包管理器、构建命令和现有测试。
2. 运行原有 build，记录真实结果。定位主动单词查询、短语查询、PDF/EPUB/字幕查询、AI makeAIRequest/配置、TTS 获取字节、后台消息与脚本加载顺序；输出入口函数及文件/行号，不只列文件名。
3. 重点检查 a3/a4 的 window/tooltip 状态、遗留自动 AI 请求以及已知词更新副作用；注明新流程应复用或抑制的路径，避免一次查询生成两份语境结果。
4. 只读探测本机 AnkiConnect：version、牌组/模型列表、可用 profile/反射能力和返回形状。核对拟用 add/update/media API 文档与当前安装版本。
5. 记录实际可用的 Chrome/Edge、Firefox 测试方式和至少一个音频导出渠道；写 docs/anki-phase1/BASELINE.md 与 CAPABILITIES.md。

## 必须验证

- A00：基础构建结果与关键入口图可复查。
- A01：实际 Anki 能力与未验证能力明确区分；未运行不能写通过。

## 明确禁止

不增加业务功能、不写真实 Anki 笔记、不把空代码搜索结果当作不存在功能的证明。

## 完成条件

上述行为通过相应测试，既有回归未倒退，修改限于本任务范围。将真实命令/结果/未运行项写入 `PROGRESS.md`，独立提交。若实际API或构建能力与设计不符，记录证据和最小调整；不得静默改产品行为或删验收项。

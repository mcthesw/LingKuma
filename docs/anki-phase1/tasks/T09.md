# T09 — 实现幂等创建与未知结果恢复

状态：TODO。
依赖：T03, T04, T05, T08。

## 开始前

阅读 `START_HERE.md`、本任务相关的 `SPEC.md` / `ARCHITECTURE.md` / `API_CONTRACTS.md` 条款，以及 `ACCEPTANCE.md` 中下列用例。先检查依赖任务证据，不凭口头报告跳过。

## 修改范围

sync-service.js 的创建分支、Anki fake、集成测试。

## 实施步骤

1. 写前核对目标并按CaptureId查询；精确notesInfo校验。0/1/多结果必须分别处理。
2. 落盘pendingWrite后调用addNote；CaptureId为第一字段、allowDuplicate=false；不同语境相同词均可创建。
3. 成功后读取核实，再标记wasLinked/baseFields/noteIdHint/lastVerifiedAt；不能以HTTP返回就标成功。
4. 模拟add已成功但响应丢失/worker中止；恢复先查ID和intendedFields，匹配则确认，不再次新建。
5. 曾关联但查无记录时remote_missing；同ID多个记录时conflict；重复字段错误重新查找。

## 必须验证

- A21：正常自动创建与有效内容门槛。
- A22：add成功后丢响应/落盘失败/进程中止不重复创建。
- A23：词重复但语境不同可创建；多匹配不任取第一条。

## 明确禁止

不调用guiAddCards；不把重试简单实现成再调用addNote。

## 完成条件

上述行为通过相应测试，既有回归未倒退，修改限于本任务范围。将真实命令/结果/未运行项写入 `PROGRESS.md`，独立提交。若实际API或构建能力与设计不符，记录证据和最小调整；不得静默改产品行为或删验收项。

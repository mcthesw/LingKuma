# T13 — 接入点词即保存及无阻塞弹窗

状态：TODO。
依赖：T06, T08, T11, T12。

## 开始前

阅读 `START_HERE.md`、本任务相关的 `SPEC.md` / `ARCHITECTURE.md` / `API_CONTRACTS.md` 条款，以及 `ACCEPTANCE.md` 中下列用例。先检查依赖任务证据，不凭口头报告跳过。

## 修改范围

content-adapter.js；T00定位的主动查词入口最小改动。

## 实施步骤

1. 在主动查询的语义入口冻结真实上下文并调用capture.lookup；不要在任意click/showTooltip/render上制卡。
2. 不新增必经“加入Anki”/词义选择/确认按钮；原词汇状态、字典、发音控件保留。
3. 弹窗即时显示，持久化ACK后显示本地保存，verified后显示本机Anki写入；状态提示不抢焦点、不每条弹对话框。
4. 当前语境解释使用持久化任务的结果；抑制该查询分支重复的遗留自动AI请求，非重叠词典/深度分析保持原行为。
5. 按lookupSessionId/captureId处理订阅；重复打开读记录，关闭popup/换词不取消已保存任务。

## 必须验证

- A32：真实点击一次即查词并最终制卡，额外保存次数为0。
- A18/A19：去重和异步防串词。
- A33：悬浮、重绘、发音、管理记录、关闭按钮不制卡。

## 明确禁止

不能把新功能藏在保存按钮后；不能全局劫持网页链接或编辑器点击。

## 完成条件

上述行为通过相应测试，既有回归未倒退，修改限于本任务范围。将真实命令/结果/未运行项写入 `PROGRESS.md`，独立提交。若实际API或构建能力与设计不符，记录证据和最小调整；不得静默改产品行为或删验收项。

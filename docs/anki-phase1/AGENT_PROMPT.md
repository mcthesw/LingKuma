请实施本仓库的 LingKuma → Anki 一期功能。

先读 docs/anki-phase1/START_HERE.md、SPEC.md、ARCHITECTURE.md、API_CONTRACTS.md 和 TASKS.md。若文档放在其他目录，只调整这里的路径，不改变内容。

需求已经确认，不再把“自动保存还是点加入 Anki”作为产品问题重新询问。核心行为是：用户主动点击查词即自动保存当前语境；有效释义就绪后自动写入本机 Anki；同一摘录重开记录，不重复制卡；新语境自动新建。没有额外保存按钮作为必经步骤。

默认卡片为词语＋原句在正面，释义＋原句在背面，不默认挖空。Anki 自己维护复习和跨设备同步，本插件不能调用 sync 或复习状态修改接口。

现在只执行最早未完成且依赖已完成的一个任务，从 T00 开始。按照任务里的边界、接口、测试和禁止事项实施。修改前检查本地未提交改动，不覆盖用户改动，不把仓库重置到文档引用的远端 SHA。

不得全局重构、大规模改格式、引入前端框架、重写高亮/词汇系统、加入分布式同步、为每个任务另建一套 client/store/状态枚举。一个 Anki client，一个业务 store，一个消息协议。

每一步先补测试，随后实现并跑测试。真实 Anki 测试只触碰专用测试数据。不要拿测试通过的 mock 当作真实 Anki 的验证结果。

任务完成后更新 PROGRESS.md，并报告：变更文件、行为变化、测试命令和结果、仍未运行的验收、提交号。每个任务单独提交。不要把后续多个任务一并顺手实现。

遇到实现限制，给出实际代码/API证据及最小修正。不得通过降低验收标准、吞掉错误、静默跳过音频/Firefox/恢复测试来宣布完成。
